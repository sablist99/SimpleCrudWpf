<UserControl x:Class="WpfFrontCore.View.CrudTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border BorderBrush="Gray" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="5" Grid.Row="0">
                <Button Content="Добавить" Margin="0,0,10,0" Command="{Binding AddCommand}" />
                <Button Content="Редактировать" Margin="0,0,10,0" Command="{Binding EditCommand}" />
                <Button Content="Удалить" Margin="0,0,10,0" Command="{Binding DeleteCommand}" />
            </StackPanel>

            <DataGrid 
                x:Name="DataGridControl"
                Grid.Row="1"
                AutoGenerateColumns="False" 
                ItemsSource="{Binding TableData}" 
                SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                IsReadOnly="True">
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
